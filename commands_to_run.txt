bagi requirements.txt si fisierul .py in acelasi folder si dupa dai comenzile:

python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt


python person_detector_server.py --host 0.0.0.0 --port 8080

to conectezi de pe laptop pe
http://<UNO-Q-IP-ADDRESS>:8080

ca sa afli parola: hostname -I